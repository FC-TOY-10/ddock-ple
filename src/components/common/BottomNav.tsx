import { styled } from 'styled-components'

export const BottomNav = () => {
  return <Navigation>BottomNav</Navigation>
}

const Navigation = styled.nav`
  width: 100%;
  height: 64px;
  border-color: white;
  box-shadow: -4px 0px 20px rgba(0, 0, 0, 0.19);
`
